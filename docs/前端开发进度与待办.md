# Sebastian 前端开发进度与待办

更新时间: 2026-06-11

## 1. 当前状态概览

结论: 前端 MVP 已可用，具备核心业务页面与 API 对接能力；当前重点从“功能可见”转向“联调稳定性、可配置性、体验一致性”。

## 2. 已完成内容

### 2.1 工程与结构

- [x] React + TypeScript + Vite 前端工程初始化完成。
- [x] Tailwind 样式体系与基础 UI 类（button/input/card）已使用。
- [x] 模块化目录完成：`components`、`services`、`types`、`data`。
- [x] `App.tsx` 已实现多标签导航与用户 ID 注入。

### 2.2 功能页面

- [x] 库存管理页面：列表、新增、数量调整、临期提醒。
- [x] 菜谱推荐页面：表单输入、MCP `recipe.recommend` 调用、结果渲染。
- [x] 健康分析页面：MCP `health.analyze` 调用、BMI 与建议展示。
- [x] 厨具检查页面：MCP `equipment.check` 调用、缺失厨具展示。
- [x] 记忆检索页面：记忆保存 + MCP `search.answer` 检索结果展示。

### 2.3 数据访问层

- [x] axios 客户端封装完成（`src/services/api.ts`）。
- [x] Inventory、Agent、Agent Tools、Search、MCP API 分层封装完成。
- [x] MCP 调用使用泛型定义，结果结构支持 `_audit`。

### 2.4 类型与默认数据

- [x] 核心业务响应类型定义完成（库存、推荐、健康、设备、搜索）。
- [x] 开发态默认测试数据与默认用户 ID 已配置。

## 3. 当前已识别问题

### P0（必须优先修复）

- [x] 前后端默认端口不一致（已修复）
  - 结果：前端默认地址已对齐 `http://127.0.0.1:8000/api`。
  - 实现：`src/services/api.ts` 改为读取 `VITE_API_BASE_URL`，默认回退到 `8000`。

### P1（建议本周完成）

- [x] API_BASE_URL 已改为 Vite 环境变量读取（P0 已完成）。
- [x] 全局错误提示已统一（axios interceptor + 错误码映射 + `getFriendlyError`）。
- [x] 加载态与空态已抽离公共组件并在页面接入。

### P2（体验与质量增强）

- [x] 前端自动化测试已补齐基础能力（Vitest 组件测试 + Playwright E2E）。
- [x] API 错误码映射已统一处理（axios interceptor + 错误文案映射）。
- [x] 可观测字段已在 MCP/Agent 结果区展示（trace_id 可见）。

## 4. 待办清单（按优先级）

## 4.1 P0（1 天）

- [x] 切换 `API_BASE_URL` 到 Vite 环境变量 `VITE_API_BASE_URL`。
- [x] 提供 `.env.development` 默认值并与后端端口对齐。
- [x] 在 README/使用指南补充前端环境变量配置说明。

## 4.2 P1（2-3 天）

- [x] 增加统一错误处理层（axios interceptor + 业务提示映射）。
- [x] 抽离通用状态组件：`LoadingState`、`EmptyState`、`ErrorState`。
- [x] 优化表单校验（数字边界、必填提示、禁用态一致性）。

## 4.3 P2（3-5 天）

- [x] 引入 Vitest + React Testing Library，补关键组件测试。
- [x] 引入 Playwright 覆盖 1 条端到端主链路。
- [x] 在 MCP/Agent 结果区域展示 trace_id，支持问题排查。

## 5. 建议排期

1. Day 1: 端口与环境变量修复（P0）。
2. Day 2-3: 统一错误处理与状态组件（P1）。
3. Day 4-5: 测试能力与可观测增强（P2）。

## 6. Dev Ready（前端侧）验收标准

- [x] 首次启动不改代码可直接联通后端（API 地址走环境变量，默认对齐 8000）。
- [x] 五个主功能页面均可成功发起请求并展示结果。
- [x] 后端返回 4xx/5xx 时，页面有一致且可理解的错误提示（统一 interceptor）。
- [x] 关键流程（库存 -> 推荐/分析）可重复验证（Vitest 4 tests passed）。
- [x] E2E 主链路在真实浏览器通过（需 `npx playwright install` 后执行 `npm run test:e2e`）。

## 7. 相关文件

- `frontend/src/App.tsx`
- `frontend/src/services/api.ts`
- `frontend/src/services/error.ts`
- `frontend/src/components/InventoryManager.tsx`
- `frontend/src/components/RecipeRecommender.tsx`
- `frontend/src/components/HealthAnalyzer.tsx`
- `frontend/src/components/EquipmentChecker.tsx`
- `frontend/src/components/MemorySearch.tsx`
- `frontend/src/components/common/ErrorState.tsx`
- `frontend/src/components/common/LoadingState.tsx`
- `frontend/src/components/common/EmptyState.tsx`
- `frontend/src/components/__tests__/InventoryManager.test.tsx`
- `frontend/src/components/__tests__/MemorySearch.test.tsx`
- `frontend/tests/e2e/memory-search.spec.ts`

## 8. 测试执行说明

```bash
# 单元/组件测试（无需后端）
npm run test

# E2E 测试（需先安装浏览器，首次执行）
npx playwright install --with-deps chromium
npm run test:e2e
```
