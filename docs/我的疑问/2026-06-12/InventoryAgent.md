InventoryAgent中

InventoryAdjust是否被使用了，在用户确认进行菜谱并确认使用库存中的食材后是否会从库存中减少。

有没有用户确认进行菜谱并确认使用库存中的食材这个功能
（

前端应该在对话或者菜谱推荐页面保留当历史，默认显示当天的历史，其他历史按照时间在左边的框栏中按（年份-月份-第几周-具体日期命名）

在回答的菜谱推荐后，左边显示一个确认使用的按钮表示用户进行了这个菜谱的制作，并对库存中的进行减少，注意要可以恢复状态（库存的状态）
可以在对话窗口中显示为一个窗口，显示会使用哪些库存内容，可以进行加减，然后可以进行确认。注意可以恢复状态

）

前端对话和菜谱推荐页面和在一起。

上面是一些常用按钮就像菜谱推荐页面一样，但是下面就是前端对话


