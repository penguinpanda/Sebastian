# Sebastian 开发进度与待办

更新时间: 2026-06-11

说明: 本文档仅记录“未完成工作”。已完成能力请查看 `docs/项目开发现状总览.md`。

## 1. 本轮目标（Dev Ready）

1. 文档可执行: 新同学可按文档完成冷启动与回归。
2. 链路可验证: 关键 API 与异步任务可稳定运行。
3. 风险可定位: 依赖异常可通过 readiness 与日志快速排查。

## 2. P0（必须完成）

- [ ] 认证最小闭环（建议先 header/user_id，再升级 JWT）
	- 负责人: 后端
	- 估时: 6h
	- 验收: 未携带身份信息时关键写接口返回 401/403。

- [ ] LLM 失败降级策略（超时/网络异常不直接 500）
	- 负责人: 后端
	- 估时: 4h
	- 验收: DeepSeek 异常时 `/api/agent/chat` 返回可解释降级响应。

- [ ] 输入校验加固（库存与 Agent 请求边界）
	- 负责人: 后端
	- 估时: 3h
	- 验收: 非法参数返回一致错误码与错误结构。

- [ ] 前端 API 地址环境变量化
	- 负责人: 前端
	- 估时: 2h
	- 验收: 支持通过 Vite 环境变量切换后端地址。

- [ ] 关键 E2E 流程测试（库存 -> Agent -> 任务状态）
	- 负责人: 测试/后端
	- 估时: 8h
	- 验收: 一条端到端流程可自动化回归。

## 3. P1（稳定性增强）

- [ ] 数据库连接池参数与超时策略文档化并校验
- [ ] 库存并发调整竞态控制（乐观锁或等价方案）
- [ ] Elasticsearch 不可用时搜索优雅降级
- [ ] Celery 失败通知消费策略（至少落地一个消费端）

## 4. P2（可运维能力）

- [ ] 指标导出（请求耗时、错误率、队列长度）
- [ ] 日志结构统一为可检索格式
- [ ] 建立最小性能基线（并发、P95/P99）

## 5. 本周排期建议（5-7 天）

1. Day 1-2: 认证 + 前端环境变量 + 输入校验
2. Day 3: LLM 降级 + ES 降级
3. Day 4: E2E 测试 + 文档回归
4. Day 5-7: 稳定性增强 + 基线验证

## 6. 阻塞与风险

1. 外部 LLM 可用性波动会影响 Agent 接口稳定性。
2. 当前无完整生产安全策略，本轮仅覆盖 Dev Ready。
3. 若 Elasticsearch 环境不稳定，搜索相关验收需允许降级路径。
